# 전체 격자 크기 설정
Rows = 11
Cols = 19


for r in range(Rows):           # 행 (0 ~ 9)
    for c in range(Cols):       # 열 (0 ~ 18)

    # 1. 맨윗줄 만들기
        if r == 0:
            if 3 <= c <= 5:
                print("#", end="")
            else:
                print(" ", end="")


    # 2줄 만들기
        elif r == 1:
            if 4 <= c <= 7:
                print("#", end="")
            else:
                print(" ", end="")


    # 3줄 만들기
        elif r == 2:
            if (4 <= c <= 8) or (16 <= c <= 18):
                print("#", end="")
            else:
                print(" ", end="")


    # 4줄  만들기
        elif r == 3:
            if (5 <= c <= 9) or (14 <= c <= 17):
                print("#", end="")
            else:
                print(" ", end="")


    # 5줄  만들기
        elif r == 4:
            if (5 <= c <= 10) or (13 <= c <= 17):
                print("#", end="")
            else:
                print(" ", end="")


    # 6줄  만들기
        elif r == 5:
            if (6 <= c <= 12) or (12 <= c <= 17):
                print("#", end="")
            else:
                print(" ", end="")


    # 7줄  만들기
        elif r == 6:
            if (7 <= c <= 11) or (12 <= c <= 16):
                print("#", end="")
            else:
                print(" ", end="")


    # 8줄  만들기
        elif r == 7:
            if (8 <= c <= 11) or (12 <= c <= 15):
                print("#", end="")
            else:
                print(" ", end="")


    # 9줄  만들기
        elif r == 8:
            if (9 <= c <= 10) or (11 <= c <= 14):
                print("#", end="")
            else:
                print(" ", end="")


    # 10줄  만들기
        elif r == 9:
            if (10 <= c <= 10) or (11 <= c <= 13):
                print("#", end="")
            else:
                print(" ", end="")


    # 11줄  만들기
        elif r == 10:
            if (11 <= c <= 11) or (11 <= c <= 12):
                print("#", end="")
            else:
                print(" ", end="")


#설정하지 않은 나머지 줄 처리
        else:
            print(" ", end="")

#루프가 완전 끝난 뒤 줄 바꿈 (한 번)
    print( )
    
